# Project Setup Instructions

## Backend (FastAPI)

```bash
cd backend
python -m pip install -r requirements.txt
uvicorn main:app --reload



# Frontend (React - Vite)
npm install
cd project
npm run dev
